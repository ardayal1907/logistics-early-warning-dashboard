# Development / test dependencies.
#
# Kept separate from requirements.txt on purpose: the Streamlit Cloud deployment
# installs only requirements.txt, and pytest has no business in the runtime image.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest tests/ -q

# Versions mirror pyproject.toml [project.optional-dependencies].dev - that
# table is the single source of truth. Keep the two in sync when bumping.

-r requirements.txt

pytest==9.1.1
pytest-cov==7.0.0

# Property-based tests in tests/test_logistics_domain.py. Was installed by hand
# and never recorded here, so a clean checkout could not run the suite.
hypothesis==6.165.2

# Data contracts (logistics.contracts). Declared as the `contracts` extra in
# pyproject.toml rather than a runtime dependency: a serving image validates
# nothing and should not carry it.
pandera==0.32.1

# The HTTP layer (logistics.api) and the client its tests drive it with.
# Declared as the `api` extra in pyproject.toml, not a runtime dependency:
# the trainer image carries no web framework.
fastapi==0.141.1
uvicorn[standard]==0.52.1
httpx==0.28.1

# Lint and type gates. Configured in pyproject.toml ([tool.ruff], [tool.mypy])
# and enforced by .github/workflows/tests.yml.
ruff==0.15.3
mypy==1.19.1

# The LP solver for logistics.optimization (Phase 1, Block B). Declared as the
# `optimization` extra in pyproject.toml. 3.4.2 / highspy 1.12.0 were pinned
# there and neither has ever existed on PyPI.
pulp==3.3.2
highspy==1.15.1
